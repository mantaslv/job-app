import { Box, Typography } from '@mui/material';

const Home = () => {
    return (
        <Box>
            <Typography variant='h2' component='h1'>Apply-Ally</Typography>
        </Box>
    );
}

export default Home;